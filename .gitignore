# env — never commit secrets
.env

# SvelteKit build output
build/
.svelte-kit/

# Dependencies
node_modules/

# SQLite database
data/

# OS noise
.DS_Store
Thumbs.db

# Logs
*.log
.cursor/
.playwright-mcp/

# Playwright
test-results/
playwright-report/
blob-report/

# Vitest coverage (HTML/text reports)
coverage/

# Claude Code agent worktrees — local, ephemeral git worktrees for
# background subagents; never meant to be tracked (each is its own
# nested checkout of this repo).
.claude/worktrees/

# scripts/research-eval.js output — ephemeral eval runs, not meant to be
# committed (the cases that produced them live in the script itself).
.scratch/research-assistant/eval-runs/
