{
  "name": "podcast-recorder",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail \"vite dev\" \"node --env-file=.env server-ws-dev.js\"",
    "build": "vite build",
    "start": "node --env-file=.env server.js",
    "preview": "vite preview",
    "rooms": "node scripts/rooms.js list",
    "rooms:delete": "node scripts/rooms.js delete",
    "research:eval": "node --env-file=.env scripts/research-eval.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "dependencies": {
    "@sveltejs/kit": "^2.56.1",
    "bcryptjs": "^3.0.2",
    "better-sqlite3": "^12.2.0",
    "svelte": "^5.39.2",
    "ws": "^8.18.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@sveltejs/adapter-node": "^5.0.1",
    "@vitest/coverage-v8": "^4.1.11",
    "@vitest/ui": "^4.1.11",
    "concurrently": "^9.2.1",
    "vite": "^8.0.5",
    "vitest": "^4.1.11"
  }
}
